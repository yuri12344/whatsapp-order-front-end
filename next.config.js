/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'res.cloudinary.com', 'oaidalleapiprodscus.blob.core.windows.net'],
    }
}

module.exports = nextConfig
